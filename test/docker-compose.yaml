services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      target: test-app
    environment:
      - ASPNETCORE_URLS=${VAR_ENV}
    healthcheck:
      interval: 200ms
      retries: 10
      test: ["CMD", "healthchecker", "${VAR_ARG}"]
      timeout: 500ms
      start_period: 1s
    image: dotnet-healthchecker
  test:
    command: ["sh", "-c", "exit 0"]
    depends_on:
      app:
        condition: service_healthy
    image: busybox
